# Dette connue du garde-fou anti-fuite (R5) — EXPOSITIONS RÉELLES en attente de décision (Cran C).
# Format : <fichier><TAB><label><TAB><empreinte>   (lignes # = commentaires)
# empreinte = 16 premiers caractères hexadécimaux du SHA-256 de la ligne détectée (sans CR final).
#
# Ce n'est PAS l'allowlist. L'allowlist tait un FAUX POSITIF pour toujours. Une entrée ici nomme une
# FUITE RÉELLE, déjà présente, qu'on n'a pas le droit de corriger sans arbitrage : UNE occurrence de
# la ligne inscrite ressort en classe DETTE (signalée à chaque scan, non bloquante) tant qu'elle est
# identique. Toute autre occurrence — ligne différente, copie identique, autre fichier — reste FUITE
# (bloquante). Une entrée qui ne correspond plus à rien émet un CONFIG bloquant : la PR qui corrige la
# fuite retire l'entrée.
#
# Inscrites le 2026-09-24 (brief FUITE_APERCUS_CLOUDFLARE, volet II) à l'extension du garde au code
# et à la configuration : 9 entrées. 8 corrigées par le brief EXPOSITIONS_ET_GARDE (2026-09-25) :
# nom du dépôt privé (migration 012, build_etablissements_corse.py), dossier de financement
# (generer_manifeste.mjs, chemin désormais résolu à l'exécution), 4 noms du .gitignore remplacés
# par des motifs génériques (vérifié par git sur l'arborescence réelle : rien de ce qui était
# ignoré ne devient visible).

# Restent en dette, faute de motif générique qui les couvre sans les nommer ni sans ignorer aussi
# des fichiers ou répertoires publics (consigne du brief : mieux vaut une exposition connue qu'une
# publication). Arbitrage ouvert.
# Note du 2026-09-25 : arbitrage RENDU par Soleil. Les deux entrées ci-dessous RESTENT en dette, par décision.
# Les deux autres expositions relevées par la même revue, hors de ce fichier, restent en clair, par décision
# elles aussi. Plus aucun arbitrage n'est ouvert sur ce fichier. La mention « Arbitrage ouvert » ci-dessus est
# gardée comme trace.
# - un répertoire ignoré dont le nom révèle un module confidentiel ;
.gitignore	module_agronomie	9f5a55b910b075e4
# - une famille de fichiers ignorés nommée par un sigle de financement. Le garde ne la voyait pas
#   (sigle collé à « _ », invisible pour \b) jusqu'à la correction de sa frontière de mot, le
#   2026-09-24 ; un fichier de cette famille existe dans un clone local, la règle ne peut pas partir.
.gitignore	financement_feder	66ad02b560ba70a9
