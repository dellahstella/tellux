# verify_citation.py n'a aucune dependance externe : stdlib uniquement
# (urllib, json, argparse, re, pathlib, datetime).
#
# Cette absence de dependance est volontaire : minimiser le drift en sandbox
# bash et garantir l'execution sur n'importe quel Python 3.10+.
#
# Si vous souhaitez ajouter un wrapper plus convivial (habanero, crossref-commons),
# faites-le en sus, pas en remplacement.
