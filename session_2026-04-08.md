# Session Tellux — 2026-04-08

## Fichier de travail
`tellux_CORRECT.html` → `tellux.html` (livraison finale, ~526 Ko)

---

## Tâches accomplies

### P5 — Classes utilitaires Haiku CSS
16 classes utilitaires injectées avant le premier `</style>` :
`.mb-2/3/4`, `.c-acc/danger/warn/muted/xs`, `.hidden`, `.inline-block`, `.text-right`, `.dot-icon`, `.tag-pill`, `.m-list`

### P6 — closeLayerPanel()
Fonction ajoutée avant `tog()`. Appelée dans la branche de désactivation de `tog()` pour fermer le panel associé à une couche quand elle est désactivée. Cas spécial : `id==='hypo'` ferme aussi `hypo-list-panel`.

### P7 — PRECOMPUTED_ALIGNMENTS
15 alignements Broadbent 1980 calculés offline (brute-force sur SITES_REFERENCE.json).
Remplace le Monte Carlo runtime. `buildAutoAlignments()` simplifié pour lire depuis la constante.
Rebuild forcé à chaque activation de la couche (`_autoAlignBuilt=false` avant appel).

### Suppression bloc stats header
Supprimé : `<span class="db-dot" id="db-dot">` → `📡 29 mesures ANFR</span></div>`
Allège le header, info redondante avec les couches.

### CSS boutons actifs manquants
3 règles `.lbtn.on-*` ajoutées pour les couches sans retour visuel :
- `.on-aoc` : fond orange clair, texte orange foncé
- `.on-emag` : fond violet clair, texte violet
- `.on-radon` : fond orange clair, texte rouge-orange

### Énergies humaines — refactoring
Panel restructuré en 4 boutons distincts :
- `Production` (lProd — nouvelle couche, 33 sites PROD_ELECTRIQUE)
- `Réseau HT` (ancien "Énergie")
- `Réseau BT`
- `Antennes ANFR`

`buildProdLayer()` ajoutée : markers proportionnels à la puissance (r=√P×2.5), colorés par type (hydraulique, solaire, éolien…), popup avec détails.

### Bug user-select sur ouverture panel
`user-select:none` ajouté sur `.accordion-cat-header` et les en-têtes de panels latéraux. Empêche la sélection de texte accidentelle au clic.

### Séparation Diagnostic / Ajout mesure
- `prosp-panel` renommé "Diagnostic modèle" avec bannière explicative :
  *"Cliquez sur la carte pour analyser instantanément un point. Basé sur IGRF-14 · EMAG2 · BRGM · ANFR. Aucune mesure terrain requise."*
- Bouton "📡 Ajouter mesure" séparé avec formulaire dédié
- Section "Outils terrain" labelisée dans la sidebar

### Simulateur bâtiment désactivé
Bouton `disabled` + `opacity:0.4` + `cursor:not-allowed` + label `(en dev)`. Ne sera réactivé que sur demande explicite.

### Gamification — 9 bugs corrigés
1. `runAutoTests()` est async — appel corrigé avec `.then()` partout
2. Guard `xpAwarded{}` dans `_loadGameState()` — empêche double-award XP par test
3. `_hypoLocation(id)` — 40+ ancres géographiques par hypothèse (H1–H83)
4. `_hypoNarrative(id, res)` — texte explicatif en français par résultat de test
5. `_showXpToast(pts)` — toast flottant `+10 XP` à chaque gain
6. Flash barre XP : `boxShadow` temporaire sur gain
7. `renderHypo()` + `_renderXP()` appelés à la fin de `runAutoTests()`
8. `xpAwarded` initialisé dans le default de `_loadGameState()`
9. `runAutoTests()` dans `toggleHypoPanel` — pattern `.then().catch()` pour robustesse

---

## Validations
- `node --check` : exit 0 sur le JS extrait du fichier final
- Fichier livré : `tellux.html` (~526 Ko)

---

## Tâches en attente (Direction artistique v1 — non démarrées)

| Étape | Contenu |
|-------|---------|
| 1 | Variables CSS `--tx-*` + typographie Fraunces / IBM Plex Sans / JetBrains Mono |
| 2 | Header (logo texte Fraunces) + footer (ligne micro-typo) |
| 3 | Panel latéral 5 sections cardinales (🗿 📡 🪨 🌿 🔬) |
| 4 | Toggle Découverte / Exploration / Expertise (localStorage `tellux-mode`) |
| 5 | Popups 3 niveaux : titre / score / `<details>` |
| 6 | Légende dynamique bas-gauche |
| 7 | Mobile bottom sheet (max-width 768px) |
| 8 | Grammaire boutons (primaire / secondaire / danger / ghost) |
| 9 | Gamification recadrée (confetti sur 1er ajout mesure, badge 40px discret) |

---

## Rappels techniques
- Ne jamais modifier `PROD_ELECTRIQUE[]`, `SITES[]`, `CHURCHES[]`, `FAILLES_CORSE[]`
- Ne jamais toucher `calcHuman()`, `calcHeritagePiezo()`, `runAutoTests()`, `runPermaDiag()`
- Guillemets droits uniquement dans les string literals JS (pas U+2018/U+2019)
- `node --check` obligatoire avant livraison
- `SITES_REFERENCE.json` doit être déployé au même niveau que `tellux.html` sur Cloudflare Pages
