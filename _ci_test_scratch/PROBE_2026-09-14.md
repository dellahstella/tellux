# Sonde CI — 4e check requis (2026-09-14)

Fichier de test jetable. Objet unique : vérifier que le check requis
« Clés payload vs colonnes schéma (lecture seule) » (`schema-payload-guard.yml`)
se déclenche bien sur une PR qui ne touche NI `app.html`, NI `radon.html`,
NI `_migrations/` — le cas que l'ancien filtre `paths:` masquait avant son
retrait le 2026-09-13.

Cette PR est fermée sans merge dès la vérification faite. Ce fichier ne
doit jamais atteindre `main`.
